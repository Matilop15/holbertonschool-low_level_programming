#include <stdio.h>
/**
 *main - prints the number of arguments
 *@argc: number of arguments
 *@argv: content of variable
 *Return: is ceroo
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	(void)argv;

	return (0);
}
